# ============================================
# REQUIREMENTS PARA FASTAPI - NEOSON V2.0
# Backend Assíncrono para Escalabilidade
# ============================================

# --- Core FastAPI & ASGI Server ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.9

# --- Pydantic para validação ---
pydantic==2.9.2
pydantic-settings==2.6.0

# --- Templates & Static Files ---
jinja2==3.1.4
aiofiles==24.1.0

# --- CORS & Middleware ---
python-multipart==0.0.9

# --- Core LangChain Libraries (mantidas) ---
langchain==0.3.7
langchain-community==0.3.5
langchain-core==0.3.15
langchain-openai==0.2.4
langchain-google-genai==2.0.4

# --- Additional ML Libraries ---
numpy==1.24.3
scikit-learn==1.3.0
openai==1.54.3

# --- Data Handling & Environment ---
pandas==2.2.3
python-dotenv==1.0.1

# --- Database Assíncrono ---
asyncpg==0.29.0          # Driver PostgreSQL assíncrono (substitui psycopg2)
psycopg2-binary==2.9.9   # Mantido para compatibilidade com código legado
pgvector==0.2.4

# --- OCR Dependencies (mantidas) ---
pytesseract==0.3.10
pdf2image==1.17.0
pillow==10.4.0

# --- HTTP Client Assíncrono ---
httpx==0.27.2
aiohttp==3.10.10

# --- Testing (opcional mas recomendado) ---
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2  # Para testes de API

# --- Monitoring & Observability (opcional) ---
# prometheus-fastapi-instrumentator==7.0.0
# opentelemetry-api==1.27.0
# opentelemetry-sdk==1.27.0

# --- Development Tools (opcional) ---
# black==24.10.0
# ruff==0.7.3
# mypy==1.13.0

# --- Jupyter (opcional) ---
jupyter

# ============================================
# NOTAS DE MIGRAÇÃO:
# ============================================
# 1. Flask foi REMOVIDO - substituído por FastAPI
# 2. flask-cors foi REMOVIDO - CORS nativo no FastAPI
# 3. pywebview foi REMOVIDO - backend será standalone
# 4. asyncpg ADICIONADO - driver PostgreSQL assíncrono
# 5. uvicorn ADICIONADO - servidor ASGI de alta performance
# 6. pydantic ADICIONADO - validação de dados automática
# 7. aiofiles ADICIONADO - operações de arquivo assíncronas
# 8. httpx ADICIONADO - client HTTP assíncrono
#
# Para instalar:
# pip install -r requirements_fastapi.txt
#
# Para rodar em desenvolvimento:
# uvicorn app_fastapi:app --reload
#
# Para rodar em produção:
# uvicorn app_fastapi:app --host 0.0.0.0 --port 8000 --workers 4
